<views:ThemedWindow x:Class="DiskSpaceMonitor.Views.FontPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DiskSpaceMonitor.Views"
        Title="Choose a font"
        Width="420" Height="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Font" FontSize="13" Margin="0,0,0,4"/>

        <!-- Type-to-filter, so a long installed-font list stays navigable. -->
        <TextBox x:Name="FilterBox" Grid.Row="1" Margin="0,0,0,8"
                 Padding="4,3" TextChanged="OnFilterChanged"/>

        <!-- Each family is drawn in its own face, so the list is its own preview. -->
        <ListBox x:Name="FamilyList" Grid.Row="2" SelectionChanged="OnFamilySelected">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontFamily="{Binding Family}"
                               FontSize="15" Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="3" Margin="0,12,0,0" Padding="10" MinHeight="72"
                BorderThickness="1" BorderBrush="#40808080" CornerRadius="4">
            <TextBlock x:Name="Sample" TextWrapping="Wrap" VerticalAlignment="Center"
                       FontSize="18" Text="C: 512 GB — 38% Free"/>
        </Border>

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" MinWidth="72" Padding="12,5" Margin="0,0,8,0"
                        IsCancel="True" Click="OnCancel"/>
                <Button x:Name="OkButton" Content="OK" MinWidth="72" Padding="12,5"
                        IsDefault="True" Click="OnOk"/>
            </StackPanel>
        </Grid>
    </Grid>
</views:ThemedWindow>
